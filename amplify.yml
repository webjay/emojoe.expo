version: 1
# backend:
#   phases:
#     build:
#       commands:
#         - '# Execute Amplify CLI with the helper script'
#         - amplifyPush --simple
frontend:
  phases:
    preBuild:
      commands:
        - nvm use
        - npm install
    build:
      commands:
        - npm run build
  # artifacts:
  #   baseDirectory: web-build
  #   files:
  #     - '**/*'
  # cache:
  #   paths:
  #     - node_modules/**/*
  #     - $(npm root --global)/**/*
